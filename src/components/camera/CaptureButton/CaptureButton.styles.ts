import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  button: {
    backgroundColor: '#fff',
    width: 80,
    height: 80,
    borderRadius: 40,
  },
  disabled: {
    backgroundColor: '#777',
  },
});
